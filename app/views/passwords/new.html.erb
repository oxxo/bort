<%= page_title  t('passwords.title') , :h2 %>  
<% form_for @password do |f| %>
  <%= f.error_messages %>
  <fieldset>
    <legend><%= t('passwords.formtitle') %></legend>
    <ol>
      <li>
        <%= f.label :email, t('passwords.email') %>
        <%= f.text_field :email %>
      </li>
    </ol>
  </fieldset>
  <div class="buttons">
    <%= f.submit t('passwords.submitbutton') %>
  </div>
<% end %>